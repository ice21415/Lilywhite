{
    // iOS renders the foreground app's status bar inside that app process,
    // not only inside SpringBoard. Restrict injection to UIKit processes so
    // daemons are untouched while SpringBoard and foreground apps are covered.
    Filter = { Classes = ("UIStatusBar"); };
}
