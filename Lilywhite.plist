{
    // Load only where the native status-string implementation actually exists.
    Filter = {
        Bundles = ("com.apple.springboard");
        Classes = ("STUIStatusBarStringView");
        Mode = "Any";
    };
}
