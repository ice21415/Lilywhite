{
    // Use the verified SpringBoard target while inspecting its notification
    // runtime. App-process injection is deliberately not enabled here.
    Filter = { Bundles = ("com.apple.springboard"); };
}
